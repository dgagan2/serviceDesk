
type Query {
    hello: String!,
    departmetByName(name: String!): Department,
    departmentById(idDepartment: ID!): Department,
    allDepartments: [Department],
}

type Department{
    idDepartment: ID,
    departmentName: String
}
